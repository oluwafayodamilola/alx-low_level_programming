#include <stdio.h>

/**
 * main - program that prints its name, followed by a new line.
 * @argc: the total num of arguments
 * @argv: the arguments in the program
 *
 * Return: always 0;
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
}
